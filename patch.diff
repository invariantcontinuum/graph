--- site/app/Showcase.tsx
+++ site/app/Showcase.tsx
@@ -554,10 +554,10 @@
             <button type="button" className="action-button" onClick={() => graphRef.current?.fit(48)}>
               Fit
             </button>
-            <button type="button" className="action-button" onClick={() => graphRef.current?.zoomIn()}>
+            <button type="button" className="action-button" onClick={() => graphRef.current?.zoomIn()} title="Keyboard shortcut: + or =">
               Zoom In
             </button>
-            <button type="button" className="action-button" onClick={() => graphRef.current?.zoomOut()}>
+            <button type="button" className="action-button" onClick={() => graphRef.current?.zoomOut()} title="Keyboard shortcut: - or _">
               Zoom Out
             </button>
           </div>
@@ -574,14 +574,16 @@
             <button
               type="button"
               className="action-button"
               disabled={!selectedNode}
+              title={!selectedNode ? "Select a node first to add a neighbor" : undefined}
               onClick={addNeighbor}
             >
               Add Neighbor
             </button>
             <button
               type="button"
               className="danger-button"
               disabled={!selectedNode}
+              title={!selectedNode ? "Select a node first to remove it" : undefined}
               onClick={removeSelected}
             >
               Remove
@@ -683,7 +685,7 @@
             <button type="button" className="action-button" onClick={applySnapshotJson}>
               Apply Graph
             </button>
-            {snapshotErr ? <span className="error-text">{snapshotErr}</span> : null}
+            {snapshotErr ? <span className="error-text" role="alert" aria-live="assertive">{snapshotErr}</span> : null}
           </div>
           <label className="field-label" htmlFor="theme-json-editor">
             themeOverrides.json
@@ -699,7 +701,7 @@
             <button type="button" className="action-button" onClick={applyThemeJson}>
               Apply Theme
             </button>
-            {themeErr ? <span className="error-text">{themeErr}</span> : null}
+            {themeErr ? <span className="error-text" role="alert" aria-live="assertive">{themeErr}</span> : null}
           </div>
         </section>
       </aside>
