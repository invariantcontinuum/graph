/target
/pkg
node_modules/
.DS_Store
.worktrees

# vitest coverage output — regenerated on every `npm test`
coverage/

# npmrc files carry GHCR auth tokens for @invariantcontinuum scope.
# Publish workflow writes its own in CI; locally, consumers install via
# ~/.npmrc or per-checkout `.npmrc` that stays on disk only.
.npmrc

# Local-only invariant documentation tree; agents generate this but it must
# never be committed (see AGENTS.md).
docs/
